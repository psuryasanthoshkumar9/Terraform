Day-Combined Terraform project
- Ensure aws CLI credentials are configured (aws configure)
- Create lambda_function.zip from lambda_function.py (zip command given below)
- Run:
  terraform init -upgrade
  terraform apply -auto-approve
